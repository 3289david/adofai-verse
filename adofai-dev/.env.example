# adofai-dev — no secrets in this app; all vars are public config.
# Copy to .env (committed) or .env.local (local override).

# IdP base URL — used to build live OAuth endpoint links
NEXT_PUBLIC_AUTH_ISSUER="https://auth.adofai.net"
