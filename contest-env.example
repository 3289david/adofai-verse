# =============================================================================
# ADOFAI Battle — combined single-file shortcut (everything in `.env.local` locally).
# Split for sharing public defaults: `.env.public.example`, secrets: `.env.local.example`
# Developer docs hub: https://dev.adofai.net → `/dev/environment`
# =============================================================================

NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_AUTH_ISSUER="https://auth.adofai.net"
OAUTH_CLIENT_ID="adofai_online_contest"
